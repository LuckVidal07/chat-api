info:
  name: buscarConversas
  type: http

http:
  method: GET
  url: http://localhost/api/conversations
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
