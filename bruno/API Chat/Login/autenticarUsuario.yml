info:
  name: autenticarUsuario
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost/api/login
  body:
    type: json
    data: |-
      {
          "email": "lucas@test.com",
          "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
